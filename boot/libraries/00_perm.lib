--[[

    Permission Library

  NAME: /boot/libraries/00_perm.lib
  INFO: Sets up the permission- & usersystem
  AUTHOR: Piorjade

]]

--list of forbidden folders (& files)
--every file/folder is listed here and has a table as it's value, which contains informations like whether or not users have (r)ead or (w)riteaccess to that file/folder

--TODO: Finish perm library

local tForbidden = {
  ["/boot"] = {
    owner = "root",
    r = true,
    w = false,
  }
}
local _fs = _G.fs

local function loadConfig()
  local tFile, sErr = _fs.open("/etc/perm.d/permissions.conf", "r")
  if not tFile return false, sErr end
  local data = tFile.readAll()
  tFile.close()
  --TODO: Here
  data =
end
