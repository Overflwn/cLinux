local t = shell.getServ()
for _, a in pairs(t) do
	print(tostring(_))
	print(tostring(t[_].file))
end

local ok, err = shell.startServ("/sys/redn")
if ok then
	print("Success")
else
	print("Failure."..err)
end